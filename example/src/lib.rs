/// An example struct.
///
/// This is mostly so that we can check the docs.
pub struct ExampleStruct;

impl ExampleStruct{
    /// An example method.
    ///
    /// Sup.
    pub fn example_method(&self) {
        println!("Hello world");
    }
}